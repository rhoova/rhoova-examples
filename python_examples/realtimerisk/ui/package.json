{
  "name": "wrangler-ui-min",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --assets=./dist --port=8788",
    "preview": "wrangler dev --assets=./dist --port=8788",
    "deploy": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "wrangler": "4.32.0"
  }
}